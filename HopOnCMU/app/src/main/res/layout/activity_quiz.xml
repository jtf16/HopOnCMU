<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.v7.widget.Toolbar
        android:id="@+id/my_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:elevation="4dp"
        app:navigationIcon="@drawable/ic_arrow_back_white"
        app:titleTextAppearance="@style/Toolbar.TitleText">

        <RelativeLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginEnd="48dp"
            android:layout_marginRight="48dp"
            android:gravity="center_horizontal">

            <ImageButton
                android:id="@+id/arrow_left"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_toLeftOf="@id/question_number"
                android:layout_toStartOf="@id/question_number"
                android:background="@android:color/transparent"
                android:gravity="center"
                android:onClick="goLeft"
                android:src="@drawable/ic_keyboard_arrow_left_white" />

            <EditText
                android:id="@+id/question_number"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_toLeftOf="@id/slash"
                android:layout_toStartOf="@id/slash"
                android:background="@drawable/edittext_background"
                android:gravity="center"
                android:inputType="number"
                android:maxEms="2"
                android:minEms="2"
                android:padding="5dp"
                android:singleLine="true"
                android:text="1"
                android:textSize="20sp" />

            <TextView
                android:id="@+id/slash"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_toLeftOf="@id/total_question_numbers"
                android:layout_toStartOf="@id/total_question_numbers"
                android:padding="4dp"
                android:text=" / "
                android:textColor="@android:color/white"
                android:textSize="22sp" />

            <TextView
                android:id="@+id/total_question_numbers"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_toLeftOf="@id/arrow_right"
                android:layout_toStartOf="@id/arrow_right"
                android:paddingBottom="4dp"
                android:paddingTop="4dp"
                android:text="30"
                android:textColor="@android:color/white"
                android:textSize="22sp" />

            <ImageButton
                android:id="@+id/arrow_right"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_alignParentRight="true"
                android:background="@android:color/transparent"
                android:gravity="center"
                android:onClick="goRight"
                android:src="@drawable/ic_keyboard_arrow_right_white" />

        </RelativeLayout>

    </android.support.v7.widget.Toolbar>

    <android.support.v4.view.ViewPager xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/submit_btn"
        android:layout_below="@id/my_toolbar" />

    <Button
        android:id="@+id/submit_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:onClick="submit"
        android:text="@string/quiz_submit_btn" />

</RelativeLayout>